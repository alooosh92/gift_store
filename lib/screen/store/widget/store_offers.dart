import 'package:flutter/material.dart';

class StoreOffers extends StatelessWidget {
  const StoreOffers({super.key, required this.id});
  final String id;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
